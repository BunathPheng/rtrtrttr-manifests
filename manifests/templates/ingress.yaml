---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rtrtrttr                  # Ingress name
  labels:
    app: rtrtrttr
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "rtrtrttr.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rtrtrttr               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
